# Optical flow metric runtime dependencies.
# Recommended setup (conda):
#   conda create -n optical_flow python=3.10 -y
#   conda activate optical_flow
#   conda install pytorch torchvision pytorch-cuda=12.1 -c pytorch -c nvidia
#   pip install -r requirements.txt
#
# CPU-only:
#   conda install pytorch torchvision cpuonly -c pytorch
#   pip install -r requirements.txt

numpy>=1.21.0
scipy>=1.7.0
opencv-python>=4.5.0

# If PyTorch is not installed via conda, these lines install CPU wheels by default.
# For GPU, prefer the conda install command above.
torch>=1.10.0
torchvision>=0.11.0
