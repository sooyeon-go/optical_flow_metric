# Optical flow metric dependencies (pip only).
# Install PyTorch with conda FIRST, then install this file.
#
#   conda create -n optical_flow python=3.10 -y
#   conda activate optical_flow
#   conda install pytorch torchvision pytorch-cuda=12.1 -c pytorch -c nvidia
#   pip install -r requirements.txt
#
# Do NOT pip install torch/torchvision here. Mixed pip+conda torch often causes:
#   ImportError: libtorch_cpu.so: undefined symbol: iJIT_NotifyEvent

numpy>=1.21.0,<2.3
scipy>=1.7.0
opencv-python>=4.5.0
